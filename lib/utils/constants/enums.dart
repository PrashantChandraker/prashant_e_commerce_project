enum TextSizes { small, medium, large, defaultSpace }

enum OrderStatus { processing, shipped, delivered, pending, cancelled }

enum PayementMethods {
  paypal,
  googlepay,
  visa,
  mastercard,
  creditcard,
  paystack,
  razorpay,
  patym,
  applepay
}

enum ProductType{ single, variable}